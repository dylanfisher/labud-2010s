<div class="site-container">
  <div class="container-fluid">
    <div class="row">
      <%= content_tag :div, class: "markdown #{'body-text-large' if block.large?} #{block.large? ? 'col-md-10 offset-md-1' : 'col-md-8 offset-md-2'}" do %>
        <div class="pullout-card-block__content">
          <%= md block.text %>
        </div>
      <% end %>
    </div>
  </div>
</div>
